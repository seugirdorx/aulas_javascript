let f, res;

f = prompt ("Digite o valor de Fahrenheit para converter: ");

function Conversão (f){
    res = (f- 32) * 5/9
    return alert ("O resultado da conversão de Fahrenheit para Celsius é: " + res);
}

Conversão (f);