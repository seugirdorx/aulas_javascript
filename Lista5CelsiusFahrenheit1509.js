let c, res;

c = prompt ("Digite o valor de Celsius para converter: ");

function Conversão (c){
   res = c * 9 / 5 + parseInt(32)
    return alert ("O resultado da conversão de Celsius para Fahrenheit é: " + res);
}

Conversão(c)

